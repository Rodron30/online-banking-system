services:
  - type: web
    name: online-banking-system
    runtime: docker
    plan: free
    region: singapore

    envVars:
      - key: DB_URL
        sync: false

      - key: DB_USERNAME
        sync: false

      - key: DB_PASSWORD
        sync: false